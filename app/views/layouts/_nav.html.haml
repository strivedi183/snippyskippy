.fixed
  %nav.top-bar
    %ul.title-area.left
    -if @auth.present?
      %li.name
        %h1
          %a= link_to(@auth.name + ' | Logout', '/login', :method => :delete, :id => 'logout_btn')
    -if !@auth.present?
      %li.name
        %h1
          %a{"data-dropdown" => "drop", :href => "#"}= 'Login'
          #drop.f-dropdown.content
            %li#login_form
              = form_tag '/login' do
                = text_field_tag :email, nil, :autofocus => true, :placeholder => 'Email'
                = password_field_tag :password, nil, :id => 'password_field', :placeholder => 'Password'
                .login_btn
                  = submit_tag 'Login', :id => 'login_btn', :class => 'button radius success'
      %li.name
        %h1
          %a{"data-reveal-id" => "register", :href => "#"}= 'Register'

#modals
  #register.reveal-modal
    %h2 Info
    %h4 More Info
    %h5 Even More Info
    %a{"data-reveal-id" => "stylist", :href => "#", :class => 'button radius secondary'} Stylist
    %a{"data-reveal-id" => "client", :href => "#", :class => 'button radius secondary'} Client
    %a.close-reveal-modal X

  #stylist.reveal-modal
    = render(:partial => 'stylists/form')
    %a.close-reveal-modal X

  #client.reveal-modal
    = render(:partial => 'clients/form')
    %a.close-reveal-modal X


